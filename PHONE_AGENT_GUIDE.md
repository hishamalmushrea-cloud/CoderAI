# دليل تحويل المشروع إلى وكيل هاتف ذكي (AI Phone Agent Architecture Guide)

هذا الدليل يوضح البنية الهندسية الشاملة والميزات البرمجية التي تمت إضافتها إلى مشروع **CoderAI / AndCode** لتحويله من مجرد "وكيل برمجي محلي" إلى **وكيل شخصي ومساعد صوتي متكامل لنظام أندرويد (Personal AI Phone Agent)** شبيه بسيري ومساعد جوجل ولكن بقوة نماذج الذكاء الاصطناعي التوليدي ونظام الوكلاء المستقلين (Autonomous Agents).

---

## 1. مخطط تدفق العمل الصوتي (Voice Pipeline Architecture)

```
       [ المستخدم ينطق الكلمة المفتاحية (Hey Siri / يا مساعد) ]
                                 │
                                 ▼
                   WakeWordService (نموذج Vosk الصوتي المحلي)
                                 │
                                 ▼
             SpeechRecognizerManager (تحويل الصوت إلى نص STT)
                                 │
                                 ▼
                LLM / Agent Engine (Claude / OpenCode / SLM)
                   قراءة الموجه العام and-code-agent-context.md
                                 │
                                 ▼
                   استدعاء أداة (MCP Tool Call):
             device_send_whatsapp / device_make_call / ...
                                 │
                                 ▼
            andcode-device-mcp.py (خادم بروتوكول سياق النموذج MCP)
                                 │
                   (ملف الجسر في .and-code/device-bridge/)
                                 ▼
                      DeviceBridge.kt (Kotlin)
                                 │
                                 ▼
                   AndroidDeviceBridge.kt (النظام الأصلي)
        ┌────────────────────────┼────────────────────────┐
        ▼                        ▼                        ▼
  واجهات أندرويد         خدمة إمكانية الوصول           نظام الجدولة
  (Intents/MediaStore)   (AccessibilityService)       (AlarmManager)
        │                        │                        │
  فتح التطبيق / الاتصال      النقر والكتابة التلقائية     تنفيذ بعد 10 دقائق
        └────────────────────────┼────────────────────────┘
                                 │
                                 ▼
                 توليد الرد الصوتي للمستخدم (TTS)
                    AndroidTTS / CloudTTS مع Barge-in
```

---

## 2. المكونات البرمجية التي تم بناؤها (Implemented Components)

### أ. جسر النظام وأدوات الهاتف (`AndroidDeviceBridge.kt`)
ملف Kotlin مخصص يربط أندرويد بطلبات الذكاء الاصطناعي:
1. **فتح التطبيقات بالاسم (`launchApp`):**
   * يتعرف على أسماء التطبيقات الشائعة باللغتين العربية والإنجليزية (مثل: "واتساب", "whatsapp", "يوتيوب", "كاميرا", "الاستوديو", "الإعدادات").
   * يقوم بالبحث التلقائي في قائمة التطبيقات المثبتة (`PackageManager`) ومطابقة أسماء الحزم وأسماء التطبيقات ثم تشغيلها فوراً.
2. **إجراء المكالمات والبحث في جهات الاتصال (`makePhoneCall`, `searchContacts`):**
   * البحث عن الاسم في دليل جهات الاتصال (`ContactsContract`).
   * استخراج رقم الهاتف وإطلاق الاتصال المباشر (`ACTION_CALL`) أو فتح لوحة الاتصال (`ACTION_DIAL`).
3. **إرسال رسائل واتساب (`sendWhatsAppMessage`):**
   * دعم البحث عن اسم الشخص وتحويله لرقم دولي نظيف.
   * إطلاق intent الواتساب المباشر عبر الرابط `https://api.whatsapp.com/send?phone=...&text=...`.
   * التنسيق مع خدمة إمكانية الوصول للضغط التلقائي على زر "إرسال".
4. **البحث في ملفات وفيديوهات الهاتف (`searchMedia`):**
   * استعلام فوري من قاعدة بيانات الوسائط بأندرويد (`MediaStore.Video`, `MediaStore.Images`, `MediaStore.Audio`, `MediaStore.Files`).
   * بحث احتياطي بالمسارات الفعلية (`/sdcard/Download`, `/sdcard/DCIM`, `/sdcard/Movies`, `/sdcard/Documents`).
   * إرجاع تفاصيل كاملة: اسم الملف، مساره الدقيق، حجمه بصيغة مقروءة (MB/GB)، تاريخ التعديل، والنوع.
5. **المؤقتات والجدولة الزمنية للمهام (`setTimer`, `scheduleDelayedAction`):**
   * ضبط مؤقتات العد التنازلي التلقائية في تطبيق الساعة (`AlarmClock.ACTION_SET_TIMER`).
   * جدولة المهام المؤجلة (مثل "أرسل رسالة بعد 10 دقائق") عبر `AlarmManager.setExactAndAllowWhileIdle` ومستقبل البث `DelayedActionReceiver`.
6. **التحكم بالعتاد وحالة الجهاز (`controlHardware`, `getDeviceStatus`):**
   * تشغيل وإطفاء الكشاف/المصباح (Flashlight) عبر `CameraManager.setTorchMode`.
   * قراءة نسبة البطارية وحالة الشحن والمساحة التخزينية المتبقية وتفاصيل الهاتف.

---

### ب. خدمة إمكانية الوصول للأتمتة بدون حاسوب (`AndCodeAccessibilityService.kt`)
* كانت النسخة الأصلية تعتمد على سكربتات ADB التي تتطلب تفعيل Wireless Debugging واقتران مع الحاسوب.
* تم استبدال/تعزيز ذلك بخدمة إمكانية الوصول (`AccessibilityService`):
  * تستطيع استكشاف عناصر الواجهة المعروضة على الشاشة وقراءة النصوص.
  * النقر التلقائي على الأزرار بالاسم أو بالمعرّف (مثل الضغط على زر "إرسال" في واتساب بعد فتحه وتعبئة الرسالة تلقائياً).
  * الكتابة داخل حقول النصوص والتمرير على الشاشة.

---

### ج. خادم بروتوكول MCP للوكيل (`andcode-device-mcp.py` & `DeviceBridge.kt`)
* تم بناء خادم MCP قياسي يعمل داخل بيئة Linux (PRoot) ويتصل بالهاتف عبر جسر الملفات الآمن (`.and-code/device-bridge`).
* الأدوات المعروضة للوكيل الذكي:
  * `device_launch_app`: تشغيل التطبيقات.
  * `device_make_call`: الاتصال بجهة اتصال أو رقم.
  * `device_send_whatsapp`: إرسال رسالة واتساب مع خيار الإرسال التلقائي.
  * `device_send_sms`: إرسال رسائل SMS نصية.
  * `device_search_media`: البحث عن ملفات/فيديوهات/صور.
  * `device_open_file`: فتح أي ملف على الجهاز بمستعرضه الافتراضي.
  * `device_set_timer`: ضبط مؤقت.
  * `device_schedule_action`: جدولة مهام بعد مدة زمنية (مثلاً 10 دقائق).
  * `device_control_hardware`: التحكم بالمصباح/الكشاف.
  * `device_get_status`: جلب حالة الهاتف (البطارية، الذاكرة، الوقت).
  * `device_screen_click`, `device_screen_type`, `device_screen_dump`: أتمتة الشاشة عبر Accessibility.

---

## 3. أمثلة لأوامر صوتية وتصرف الوكيل (User Voice Scenarios)

### سيناريو 1: "افتح تطبيق واتساب"
* **تحليل الوكيل:** استدعاء أداة `device_launch_app(appName="واتساب")`.
* **تنفيذ النظام:** يقوم `AndroidDeviceBridge` بالبحث عن حزمة `com.whatsapp` وتشغيلها فوراً.
* **الرد الصوتي للمساعد:** *"جارٍ فتح تطبيق واتساب."*

### سيناريو 2: "اتصل لأحمد"
* **تحليل الوكيل:** استدعاء أداة `device_make_call(recipient="أحمد")`.
* **تنفيذ النظام:** يبحث في جهات الاتصال عن "أحمد"، يجلب رقمه، ويطلق مكالمة هاتفية فورية.
* **الرد الصوتي للمساعد:** *"جارٍ الاتصال بأحمد..."*

### سيناريو 3: "ارسل رسالة بعد 10 دقائق لعمر قله وصلت البيت"
* **تحليل الوكيل:** استدعاء أداة:
  ```json
  device_schedule_action(
    delayMinutes = 10,
    actionType = "send_whatsapp",
    parameters = {
      "recipient": "عمر",
      "message": "وصلت البيت"
    }
  )
  ```
* **تنفيذ النظام:** يقوم `AlarmManager` بجدولة مؤقت عالي الدقة بعد 10 دقائق بالضبط، وعند حلول الوقت يستيقظ المعالج ويقوم `DelayedActionReceiver` بإرسال الرسالة وإشعارك.
* **الرد الصوتي للمساعد:** *"تمت الجدولة بنجاح، سأرسل رسالة واتساب لعمر بعد 10 دقائق."*

### سيناريو 4: "ابحث عن فيديو البحر في جهازي"
* **تحليل الوكيل:** استدعاء أداة:
  `device_search_media(query="بحر", mediaType="video")`
* **تنفيذ النظام:** استعلام قاعدة بيانات أندرويد ومجلدات `/sdcard/DCIM` و `/sdcard/Movies`، وإرجاع قائمة الفيديوهات.
* **الرد الصوتي للمساعد:** *"وجدت مقطع فيديو باسم 'رحلة_البحر.mp4' بحجم 120 ميجابايت، هل ترغب في أن أقوم بفتحه؟"*

---

## 4. إضافات مستقبلية مقترحة لرفع كفاءة الوكيل (Roadmap & Enhancements)

1. **الاستماع للإشعارات والرد التلقائي (`NotificationListenerService`):**
   إتاحة قراءة إشعارات واتساب وتيليجرام الواردة واقتراح ردود ذكية أو قراءتها صوتياً أثناء القيادة.
2. **الذكاء المحلي الصامت والسريع (On-Device Local SLM):**
   دمج مكتبة `llama.cpp` لتشغيل نماذج صغيرة مثل (Llama 3.2 1B أو Gemma 2 2B) لأداء العمليات الروتينية (كالمؤقتات وتشغيل التطبيقات والكشاف) دون الحاجة لاتصال بالإنترنت وفي زمن استجابة أقل من 200ms.
3. **الذاكرة التراكمية الشخصية (Long-Term Personal Memory):**
   تخزين تفضيلات المستخدم والعلاقات الاجتماعية (من هو أخوك، عنوان منزلك، مطعمك المفضل) في قاعدة بيانات SQLite محلية مشفرة.
